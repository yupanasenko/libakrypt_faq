 Ошибки:
  1. Реализация ASN1
     - некорректное чтение случайных данных в качестве der-последовательностей (аварийный останов)
     - чтение очень длинных .pem файлов (например, без символа возврата каретки)

  2. Запланировать версию aktool 0.2
      сделать общий пул глобальных переменных, взяв за основу фрагмент из aktool_key
       сделать общие параметры для командной строки
       разобраться с паролями, очень похоже, что там переизбыток функций,
        может стоит сделать еще один параметр при чтении пароля (для hex)

  4. Собрать макросы для флагов сектертного/открытого ключей в один enum
     Это позволит существенно яснее двигаться по документации и проверть условия корректности

  3. Внести certificate opts в состав verify_key
     Сделать тетью (!) ревизию кода и внятно разобраться с последовательностью действий,
     сейчас код очень нестабилен и немодифицируем ((
     - в certifiate_opts убрать authirithyName.inclide_name


 План восстановления:
  1. Ключи:
      - систематизировать функции выработки производных ключей
        и привести реализации к соотвествию стандартам (где можно)
      - функция divers выработки производных ключей (ключевое дерево)

  4. Вернуть в aktool проверку целостности ( + частично)
  5. Частично вернуть в aktool симметричное шифрование файлов
  7. Встроить реализацию кривых Эдвардса и Монтгомери
  8. Сделать асимметричное шифрование

 В дальнейшем, потребуется сделать:
  - режим выработки имитовставки omac-acpkm
  - тестирование скорости работы для генераторов случайных чисел (утилита aktool)

  - сделать aead с key meshing (выработкой производных ключей)
  - при вычислении имитовставки для файлов - вырабатывать производный ключ (aktool)
  - возможно имеет смысл сделать флаг сборки, при котором
    включаются/выключаются все нестандартизированые криптографические алгоритмы    
