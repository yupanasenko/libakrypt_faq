FAQ по встраиванию
==================

В этом разделе содержится написанная в форме вопросов и ответов
"Инструкция по встраиванию" библиотеки в прикладное программное обеспечение.
Вопросы акцентируют внимание на том, что можно сделать с помощью функций библиотеки,
а ответы, по возможности, иллюстрируются примерами работоспособных программ.


Как написать самую простую программу
------------------------------------

Есть ли базовый шаблон программы?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Программа, использующая библиотеку, должна выполнить два простых действия:

  * иициализировать библиотеку с помощью функии :func:`ak_libakrypt_create()`,
  * после окончания криптографических преобразований выполнить функцию :func:`ak_libakrypt_destroy()`.

Базовый шаблон программы выглядит следующим образом (файл ``example-g01n01.c``).

.. literalinclude:: ../examples/example-g01n01.c
   :language: c
   :linenos:

Как настроить вывод сообщений об ошибках?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Вывод сообщений об ошибках называется -- ``аудит`` действий программы.
К аудиту также относится вывод сообщений о штатном выполнении таких криптографических событий,
как ввод ключей, изменение прав доступа, удаленная аутентификация и т.п.

Могу ли я использовать библиотеку в проектах на C++?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~




Как скомпилировать мою программу?
---------------------------------

Как собрать программу в консоли Linux (MacOS, FreeBSD и т.п.)?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Как добавить библиотеку в Visual Studio?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~



Как сделать ключи?
------------------


Как я могу зашифровать данные?
------------------------------


Как использовать электронную подпись?
-------------------------------------


Как контролировать целостность?
-------------------------------


Как и зачем вырабатывать случайные числа?
-----------------------------------------


Как работать с ASN.1?
---------------------

Что вообще такое ASN.1?
~~~~~~~~~~~~~~~~~~~~~~~


Дополнительные возможности библиотеки
-------------------------------------

В заключение мы рассмотрим ряд возможностей,
которые предоставляет библиотека `libakrypt-base`.
Большинство функций этой библиотеки реализованы для исключения внешних зависимостей.

Есть ли функции для вывода шестнадцатеричных значений?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Как получить доступ к домашнему каталогу?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Какие еще каталоги распознает библиотека?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Как организовать доступ к файлам?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Есть ли интерфейс к разделяемой памяти?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Если ли функции для работы со списками?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Можно ли работать с файлами конфигурации?
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Как кодировать в base64?
~~~~~~~~~~~~~~~~~~~~~~~~


Соглашения по именам примеров
-----------------------------

Все примеры из данного раздела находятся в каталоге `examples/faq` дерева
исходных текстов библиотеки.

Каждый пример имеет вид ``example-gXXnYY.c``,
где ``XX`` это номер группы примеров, а ``YY`` номер самого примера.

Ширина кода (количество символов в одной строке) не должно превышать 90.
В этом случае отображение исходного примера в `pdf`-файле не содержит переносов строк.
